# Copy to .env.local and fill in. NEVER commit real values (AGENTS.md §1.5).
# Server-only (never exposed to the client bundle):
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# AI providers (server-side extraction/explanation) — added when providers are wired:
# DEEPGRAM_API_KEY=
# AI_GATEWAY_API_KEY=

# Public (safe to expose):
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
