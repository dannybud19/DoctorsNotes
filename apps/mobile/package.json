{
  "name": "@doctorsnotes/mobile",
  "version": "0.0.0",
  "private": true,
  "main": "expo-router/entry",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "typecheck": "tsc --noEmit"
  },
  "//": "ZERO AI dependencies here — enforced by dependency-cruiser (AGENTS.md §2). Capture deps (expo-audio/expo-camera/expo-sqlite) are added with the capture feature; this scaffold only needs to boot.",
  "dependencies": {
    "@doctorsnotes/domain": "workspace:*",
    "@doctorsnotes/reconciler": "workspace:*",
    "@doctorsnotes/supabase": "workspace:*",
    "expo": "~52.0.0",
    "expo-constants": "~17.0.0",
    "expo-linking": "~7.0.0",
    "expo-router": "~4.0.0",
    "expo-status-bar": "~2.0.0",
    "react": "18.3.1",
    "react-native": "0.76.3",
    "react-native-safe-area-context": "4.12.0",
    "react-native-screens": "~4.1.0"
  },
  "devDependencies": {
    "@types/react": "~18.3.12",
    "typescript": "^5.6.3"
  }
}
